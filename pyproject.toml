[project]
name = "qwen-image-2.1-promptenhancer-mtp"
version = "1.0.0"
description = "ComfyUI nodes for Qwen-Image 2.1's official prompt enhancer, with MTP speculative decoding for faster generation."
license = { file = "LICENSE" }
classifiers = ["Operating System :: OS Independent"]
dynamic = ["dependencies"]

[tool.setuptools.dynamic]
dependencies = { file = ["requirements.txt"] }

[project.urls]
Repository = "https://github.com/mozophe/ComfyUI-Qwen-Image-2.1-PromptEnhancer-MTP"
"Bug Tracker" = "https://github.com/mozophe/ComfyUI-Qwen-Image-2.1-PromptEnhancer-MTP/issues"

[tool.comfy]
PublisherId = "mozophe"
DisplayName = "Qwen-Image 2.1 Prompt Enhancer (MTP)"
requires-comfyui = ">=0.37.0"
